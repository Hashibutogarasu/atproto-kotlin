# Local JDK pin for SDKMAN. Enable `sdkman_auto_env=true` in
# ~/.sdkman/etc/config and running `cd` into this repo will auto-switch
# to the matching Java version. CI uses `.java-version` via
# `actions/setup-java`; both files point at Java 17 to stay in sync.
java=17.0.11-zulu
