.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
!gradle/wrapper/gradle-wrapper.properties
.kotlin/
local.properties
*.iml
.idea/
.DS_Store
**/node_modules/

# Installed lexicon corpus — derived from `npx lex install` and pinned by
# CID in at-protocol-generator/lexicons.json. Reproducible on any clone via
# `cd at-protocol-generator && npx lex install --ci`.
at-protocol-generator/lexicons/

# Per-user assistant / IDE config
.gemini/
